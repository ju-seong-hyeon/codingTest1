def dfs(v, cnt, cur_n):
    global a_cnt
    if(v == n_len):
        # a.append(cur_n)
        a_cnt += 1
    else:


if __name__ == "__main__":
    n = 25114
    n = str(n)
    n_len = len(n)
    a_cnt = 0
    # cur_n = ''
    # a = []
    # dfs(0, 0, cur_n)
    # for i in a:
    #     print(i)
    # print(a_cnt)

